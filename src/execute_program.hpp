#pragma once

#include <string>
#include <vector>
#include <set>
#include "literal.hpp"

using namespace std;

bool executeBlock(int &literalIntex, int &returnValue, bool &returned);

bool executeProgram();
