(Загрузка исходного кода
(Выгрузка исходного кода
+spec: function signature: main
+spec: function signature: arguments
+spec: function call: separate context for arguments
function signature: main
function signature: arguments
function call: separate context for arguments
spec: global -number?
spec: function declaration body==block?
	Code
		1st level instruction
		function declaration
		block
		instruction
			block
			var declaration
			expression
				bin
				operand
					left unary
					right unary
					element
						number
						variable name
						function call
			return
			if
			while
		errors
			read file
			program
			module
			import
			global
			function declaration
			block
			instruction
				block
				var declaration
				expression
					bin
					operand
						left unary
						right unary
						element
							number
							variable name
							function call
				return
				if
				while
Выполнение дерева программы
					function call
	instruction
		block
		var declaration
		expression
			operand
				element
					number
					variable name
				left unary
				right unary
			bin
		return
		if
		while
