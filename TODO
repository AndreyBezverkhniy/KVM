Now-------------------------------------------

add .gitignore

hard refactoring:
rename source files without prefix "program "
execute -> executeProgram

EOF literal value = "EOFLITERAL"

expression returns not number value but {objectReference,objectValue}

Future----------------------------------------

add operator=
exchange assign to expression instruction
add unar operators +,-,! 
add inbuilt console work functions
add documentation

