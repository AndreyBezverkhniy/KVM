fcall: arg_n, instead of signature
	functions=map<<fname,argn>,<arg_names,body>>
	fcall: fname, argn, expressions
(Выполнение дерева программы
					function call
	instruction
		block
		var declaration
		expression
			operand
				simple expression
					number
					variable name
				left unary
				right unary
			bin
		return
		if
		while

