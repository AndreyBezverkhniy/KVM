(Загрузка исходного кода
(Выгрузка исходного кода
+priority
+associativity
+binOrder
+algo
+bin operator of expressions
	Code
		instruction
+			expression
+				bin
				operand
					left unary
					right unary
					element
+						number
						variable name
						function call
						( expression )
			return
			if
			while
Выполнение дерева программы
					function call
	instruction
		block
		var declaration
		expression
			operand
				element
					number
					variable name
				left unary
				right unary
			bin
		return
		if
		while
(Обработка ошибок
	Code
		errors
			literal ptr to module path string
			literal line column
			read file
			program
			module
			import
			global
			function declaration
			block
			instruction
				block
				var declaration
				expression
					bin
					operand
						left unary
						right unary
						element
							number
							variable name
							function call
				return
				if
				while
