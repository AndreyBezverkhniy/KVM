spec/code: multiline /** --*--> /**
spec: bin-->expressions not operands
spec: l/r-unary-->expression not operand
(Загрузка исходного кода
	Code
		instruction
			if
(Выполнение дерева программы
					function call
	instruction
		block
		var declaration
		expression
			operand
				simple expression
					number
					variable name
				left unary
				right unary
			bin
		return
		if
		while
(Доработка
	fcall: arg_n, instead of signature
	isOperator
	isBin
	isLUnar
	isRUnar
(Обработка ошибок
	Code
		errors
			literal ptr to module path string
			literal line column
			read file
			program
			module
			import
			global
			function declaration
			block
			instruction
				block
				var declaration
				expression
					bin
					operand
						left unary
						right unary
						simple expression
							number
							variable name
							function call
				return
				if
				while
(Выгрузка исходного кода
	global
	functions
	instruction
		block
		var declaration
		expression
			operand
				simple expression
					number
					variable name
					function call
				left unary
				right unary
			bin
		return
		if
		while
